# Dashboard (future phase)

The Next.js dashboard belongs to Phase 5. This directory is intentionally reserved so its OAuth2 session layer can be introduced without changing the API, bot, database, or tenant boundaries established in Phase 1.
